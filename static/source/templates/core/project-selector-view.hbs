<h3>Current Project</h3>
<select name="project-selector-dropdown" id="project-selector-dropdown" class="selectpicker show-tick" title='Choose a project'>
    {{#each project_list}}
        <option {{#if is_selected }}selected{{/if}} value="{{ id }}">{{ project_name }}</option>
    {{/each}}
</select>
